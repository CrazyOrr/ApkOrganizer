# The configuration for ApkOrganizer. 
# The exported APKs should be placed under your `android_project_path`, after executing the script,
# the APKs and mapping.txt would be copied to a folder named in the format of datetime under `destination_path`.
android_project_path = E:\Projects\PlayMe\version2\PlayMe
# 发布路径
#destination_path = E:\svn\安装包\Android\发布
# 测试路径
destination_path = E:\svn\安装包\Android\测试